server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps-auth:
  url: "http://localhost:8090/auth"

graph:
  base-url: http://localhost:8091/v1.0

graph-client:
  client-id: id
  client-secret: secret
  tenant-id: id


